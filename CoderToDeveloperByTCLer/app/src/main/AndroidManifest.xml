<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jinboy.codertodeveloperbytcler.java_demo.appdemo">

    <uses-permission android:name="android.permission.INTERNET" />
    <!-- 在SDCard中创建与删除文件权限 -->
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <!-- 往SDCard写入数据权限 -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.java.CollectionEnumgGnericsActivity" />
        <activity
            android:name=".ui.java.HttpURLConnectionActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.WebViewActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.java.XMLtoEntityActivity" />
        <activity android:name=".ui.JavaDemoActivity" />
        <activity android:name=".ui.AndroidUIActivity" />
        <activity android:name=".ui.MyViewActivity" />
        <activity android:name=".ui.RecyclerViewActivity" />
        <activity android:name=".ui.AndroidDemoActivity" />
        <activity android:name=".ui.androiddemo.FileOperationsActivity" />
        <activity android:name=".ui.androiddemo.SDcardOperationsActivity" />
        <activity android:name=".ui.androiddemo.SharedPreferenceActivity" />
        <activity android:name=".ui.androidui.UIActivity"></activity>
    </application>

</manifest>